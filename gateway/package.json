{
  "name": "@burrow/host-gateway",
  "private": true,
  "version": "2026.9.1",
  "type": "module",
  "description": "Standalone Burrow host gateway daemon over versioned JSON lines stdio",
  "bin": {
    "burrow-host-gateway": "./cli.mjs"
  },
  "scripts": {
    "test": "node --test *.test.mjs test/*.test.mjs ../server/index.test.mjs",
    "install:host": "./deploy/install.sh",
    "uninstall:host": "./deploy/uninstall.sh"
  }
}
